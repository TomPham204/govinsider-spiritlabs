import RegisterButton from './RegisterButton';

const storiesOptions = {
  title: 'RegisterButton',
};

export default storiesOptions;

export const RegisterButton1 = () => (
  <RegisterButton>Register Now</RegisterButton>
);
